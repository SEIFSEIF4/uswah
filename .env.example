# Uswah — Supabase project (katanaz org, eu-central-1)
# The publishable key is safe to ship in both clients; RLS is the boundary, not the key.
# Never put the service_role key here — admin writes only, from the server.

NEXT_PUBLIC_SUPABASE_URL=https://rjkbhobntyhuochdmkkx.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_mtrjLhNe8xRhQ3QJbUjxiQ_Y1PB0Aq1

# Flutter reads the same two values:
#   flutter run --dart-define=SUPABASE_URL=... --dart-define=SUPABASE_KEY=...
